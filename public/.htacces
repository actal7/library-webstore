RewriteEngine On

# Redirect all requests to index.php (for Heroku)

RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.\*)$ index.php [QSA,L]
FallbackResource /index.php
